# Bobo's World：任务拆解、契约规范与风险清单

> 版本：v0.1（需求设计稿）
>
> 本文基于当前项目的“图片资产 → 生成任务 → OSS 私有对象 → 作品档案”链路。建议把新增功能作为独立的 `bobo-world` 领域实现。

## 1. 需求边界与默认解释

### 1.1 默认发布对象

默认加入 Bobo's World 的是**二次创作成功后的生成结果图**，不是原始上传图。原图若将来允许公开，必须增加单独的“发布原图”确认。

### 1.2 发布授权

只有用户主动点击“加入 Bobo's World”并确认后，图片才公开；生成成功、下载或关闭弹窗都不能自动公开。

### 1.3 匿名规则

匿名只影响公开展示，服务端仍保存真实所有者，用于编辑、删除、投诉和审计。

- 匿名：只显示文案；
- 非匿名：显示“用户名：文案”；
- 没有文案：不显示文案区域。

### 1.4 用户按钮

聊天页点击自己的头像后打开“我的 Bobo's World”：不展示 token、API Key 或其他凭据，只展示当前用户发布的作品，并支持图片和文案的增删改查、匿名切换。

## 2. 作业大纲

### 阶段 0：需求冻结

1. 确认公开对象是否仅为生成结果图。
2. 确认文案长度、换行、链接和敏感词规则。
3. 确认匿名默认值；建议默认开启。
4. 确认“图片修改”首版是否包含替换图片。建议首版先做文案、匿名、可见性和删除。
5. 确认删除是软删除还是立即删除 OSS。建议立即隐藏、异步清理对象。

### 阶段 1：数据库与领域模型

新增 `bobo_world_item` 表，建议字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | CHAR(36) | 条目 ID |
| `tenant_id` | VARCHAR(128) | 租户隔离 |
| `user_id` | VARCHAR(255) | 真实所有者，仅服务端使用 |
| `source_job_id` | CHAR(36) | 来源生成任务 |
| `image_object_key` | VARCHAR(512) | OSS 对象键，不返回永久 URL |
| `caption` | VARCHAR(500) NULL | 可选文案 |
| `anonymous` | TINYINT | 是否匿名 |
| `visibility` | VARCHAR(16) | `PUBLIC` / `PRIVATE` |
| `status` | VARCHAR(16) | `ACTIVE` / `DELETED` / `HIDDEN` |
| `display_name_snapshot` | VARCHAR(64) NULL | 发布时用户名快照，可选 |
| `version` | INT | 乐观锁版本 |
| `created_at` / `updated_at` | DATETIME(3) | 时间 |
| `deleted_at` | DATETIME(3) NULL | 软删除时间 |

约束：

- `UNIQUE(user_id, source_job_id)`，防止重复发布；
- 文案最多 500 个 Unicode 字符；
- 只有 `image_job.status=SUCCEEDED` 的结果才允许发布；
- 公共列表只读取 `ACTIVE + PUBLIC`；
- 为 `(visibility, status, created_at)`、`(tenant_id, user_id, created_at)` 建索引。

### 阶段 2：后端 API 契约

#### 2.1 发布

`POST /api/bobo/items`

请求：

```json
{ "jobId": "生成任务 ID", "caption": "可选文案", "anonymous": true }
```

规则：必须登录；`jobId` 必须属于当前用户且已成功；服务端从 `image_job.output_object_key` 解析图片；客户端不得提交 OSS object key 或图片 URL。空白文案按 `null` 处理。重复发布建议幂等返回已有条目。

响应：

```json
{
  "itemId": "条目 ID",
  "status": "ACTIVE",
  "caption": "文案",
  "anonymous": true,
  "imageUrl": "短期签名 URL",
  "createdAt": "2026-09-23T12:00:00Z"
}
```

#### 2.2 公共世界

`GET /api/bobo/world?limit=24&cursor=...`

```json
{
  "items": [
    {
      "itemId": "条目 ID",
      "imageUrl": "短期签名 URL",
      "hasCaption": true,
      "caption": "文案或 null",
      "senderName": null,
      "anonymous": true,
      "createdAt": "2026-09-23T12:00:00Z"
    }
  ],
  "nextCursor": "下一页游标或 null"
}
```

匿名时 `senderName` 必须为 `null`；非匿名时返回当前用户名或发布时快照；不返回真实 `userId`、邮箱、token、object key。建议游标分页，不使用会漂移的页码分页。

#### 2.3 我的作品

`GET /api/bobo/items/mine?limit=24&cursor=...`

只返回当前用户的作品，可包含 `PUBLIC`、`PRIVATE`、`HIDDEN`，默认排除 `DELETED`。

#### 2.4 单条查询

`GET /api/bobo/items/{itemId}`

公共访客只能读取 `ACTIVE + PUBLIC`；所有者可读取自己的条目；无权限时返回 404，避免泄露条目存在性。

#### 2.5 修改文案/匿名/可见性

`PATCH /api/bobo/items/{itemId}`

```json
{ "caption": "修改后的文案", "anonymous": false, "visibility": "PUBLIC", "version": 3 }
```

只能修改自己的条目。`version` 不匹配返回 409；`caption=""` 表示清空；改为 `PRIVATE` 后公共列表立即排除。

#### 2.6 删除

`DELETE /api/bobo/items/{itemId}`

事务内先标记 `DELETED`，公共列表立即隐藏；OSS 原图、缩略图、缓存异步清理。删除不应删除原生成任务或聊天记录。重复删除建议幂等成功。

#### 2.7 替换图片（建议第二期）

新图仍走现有 `image-assets/upload-policy` → OSS → `complete` 链路，服务端校验新任务归属和成功状态，再用 `PUT /api/bobo/items/{itemId}/image` 替换。旧对象进入异步删除队列。首版可以只做文案、匿名、可见性和删除。

### 阶段 3：前端交互

#### 3.1 生成成功后的操作

结果图下显示：

1. **下载到本地**：使用短期签名 URL；最好由后端加 `Content-Disposition: attachment`；失败可重试。
2. **加入 Bobo's World**：打开确认弹窗，文案可选、匿名可选，明确“公开后所有访客可看到图片”；确认后调用发布 API，成功后变为“管理这张图片”。

#### 3.2 Bobo's World / 星图

- 开始页“能力”按钮替换为“Bobo's World”；
- 只展示“关于 bbb”中的“星图”功能；
- 星图读取新 `/api/bobo/world`，不扫描固定 OSS 相册作为用户作品源；
- 卡片使用缩略图，点击后放大；
- 有文案时，图片放大完成后 150–300ms 开始文案淡入/上移；
- 匿名显示文案，非匿名显示“用户名：文案”，无文案不渲染文案区；
- 支持空状态、分页、失败重试、图片失效和 `prefers-reduced-motion`。

#### 3.3 用户头像面板

显示用户名、公开作品数量和作品列表。每项可预览、编辑文案、切换匿名、切换公开/私有、删除；删除需二次确认；并发编辑冲突提示刷新。面板不显示 token。

### 阶段 4：安全与运维

1. 所有写接口使用 `resolveRequired`，不信任请求体用户 ID。
2. OSS 保持私有，只返回短期签名 URL；数据库不保存永久公网 URL。
3. 增加 `HIDDEN` 状态和管理员隐藏/举报能力。
4. 限制发布频率、单用户公开数量、图片大小和文案长度。
5. 服务端校验 MIME、文件头、解码结果和像素尺寸，不能只信扩展名。
6. 记录发布、编辑、删除、审核和越权拒绝审计事件。
7. 注销用户时先隐藏作品，再清理 OSS 和元数据。
8. 删除后清理列表缓存和签名 URL 缓存；公共图默认使用缩略图/中图。
9. 前端转义文案，禁止直接渲染 HTML；链接需采用安全协议。

### 阶段 5：验收

- 生成成功后可下载和发布；
- 未登录不能发布、编辑、删除；
- 只有明确同意的结果图进入公共世界；
- 匿名、署名、无文案三种展示规则正确；
- 删除后公共世界立即消失；
- 用户只能管理自己的作品；
- 重复发布不产生重复条目；
- 并发修改返回冲突；
- 响应不包含 token、API Key、真实内部用户 ID；
- 越权、异常 MIME、超大图片、超长文案、失败任务均被拒绝。

## 3. 关键漏洞与待确认问题

### 高风险

1. **公开范围误判**：不能直接复用现有“关于 bbb”固定 OSS 前缀；必须以数据库 `bobo_world_item` 为唯一公开清单。
2. **对象键越权**：发布请求只能提交 `jobId`，不能提交 object key 或 URL。
3. **匿名泄露**：匿名响应不得返回真实 userId、邮箱或可逆标识。
4. **签名 URL 缓存**：删除后旧 URL 可能在有效期内仍可访问；建议有效期控制在 5 分钟左右并由业务层立即隐藏。
5. **图片审核**：图片可能包含个人信息、未成年人、版权内容或恶意内容；至少预留审核状态和管理员隐藏能力。

### 中风险

1. 生成任务成功但 OSS 对象过期，列表必须逐项容错。
2. 快速重复点击会重复发布，需要唯一约束或幂等键。
3. 并发编辑会覆盖内容，需要版本号。
4. 用户改名、注销、封禁后的公开作品需要产品规则。
5. 文案包含 HTML、脚本、敏感内容或超长 Unicode，需要转义和限制。
6. 公共星图需分页，否则会造成加载慢、内存增长和签名请求过多。

## 4. 实现意见

1. 首版只发布生成结果图，原图公开放到后续版本。
2. 先完成“发布 + 文案 + 匿名 + 删除”闭环，再做图片替换和复杂编辑。
3. 数据库公开清单优先于 OSS 扫描；OSS 只存对象，数据库决定可见性。
4. 服务端永不向前端返回 token。现有登录 token 已在 HttpOnly Cookie 中，用户面板只需移除展示逻辑。
5. 用短期签名 URL 和缩略图，公共星图不要默认加载原图。
6. 把公开设计成可撤回授权：用户可随时改私有或删除，公共列表立即隐藏。
7. 发布、编辑、删除都加幂等和乐观锁，降低重复点击和并发覆盖。
8. 旧 `/api/photo-archive` 可继续提供静态“关于 bbb”内容，但 Bobo's World 使用新 API，避免数据源混杂。

## 5. 首版完成定义

生成结果页有“下载”和“加入 Bobo's World”；发布时可填写文案、选择匿名；公共星图只展示明确同意的结果图；点击图片可放大并渐显文案；头像面板可查看、编辑文案、切换匿名和删除自己的作品；不展示 token；所有写接口有鉴权、所有权校验、幂等处理和基本审计；OSS 保持私有并使用短期签名 URL。
